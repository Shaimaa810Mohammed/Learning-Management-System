@using LearningManagementSystem.ViewModels
@{
    ViewData["Title"] = "Create Quiz";
    int courseId = (int)ViewBag.CourseId;
    List<Question> questions = (List<Question>)ViewBag.questions;
}

@model QuizViewModel

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <!-- Card frame starts -->
            <div class="card shadow p-4">
                <div class="card-body">
                    <h5 class="card-title mb-4 text-center">Create Quiz</h5>
                    <form asp-controller="Quiz" asp-action="SaveCreate" method="post">
                        <div asp-validation-summary="All" class="text-danger"></div>

                        <input type="hidden" asp-for="CourseId" value=@courseId />

                        <div class="form-group mb-3">
                            <label asp-for="Title"></label>
                            <input class="form-control" asp-for="Title">
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="StartTime"></label>
                            <input class="form-control" asp-for="StartTime">
                            <span asp-validation-for="StartTime" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="EndTime"></label>
                            <input class="form-control" asp-for="EndTime">
                            <span asp-validation-for="EndTime" class="text-danger"></span>
                        </div>


                        <!--==========================================================    -->

                        <hr />
                        <h6 class="fw-bold mb-3">Select Questions</h6>

                        @if (questions == null || !questions.Any())
                        {
                            <div class="alert alert-warning">
                                No questions available for this course.
                            </div>
                        }
                        else
                        {
                            <div class="list-group">
                                @foreach (var q in questions)
                                {
                                    <label class="list-group-item d-flex align-items-start gap-2">
                                        <input type="checkbox"
                                               name="SelectedQuestionsIds"
                                               value=@q.Id
                                               class="form-check-input mt-1" />

                                        <div>
                                            <strong>@q.QuestionType</strong>
                                            <p class="mb-1">@q.QuestionText</p>
                                        </div>
                                    </label>
                                }
                            </div>
                        }

                        <button type="submit" class="btn btn-primary w-100">Create</button>
                    </form>
                </div>
            </div>
            <!-- Card frame ends -->
        </div>
    </div>
</div>

<partial name="_ValidationScriptsPartial" />